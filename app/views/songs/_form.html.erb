
<% flash.each do |type, msg| %>
  <%= content_tag :div, msg, class: "flash_#{type}" %>
<% end %>
<%= form_for(@song) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, id: 'most_recent_song_id' %>
  <%= f.label :artist %>
  <%= f.text_field :artist %>
  <%= f.submit %>
<% end %>
